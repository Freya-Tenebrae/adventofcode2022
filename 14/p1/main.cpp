#include <string>
#include <fstream>
#include <iostream>
#include <vector>

int main()
{
	std::ifstream input("input.txt");
	std::string line;

	while(std::getline(input, line))
	{
		
	}

	std::cout << " trees are visible from outside the grid";
	std::cout << total << std::endl;
	return (0);
}